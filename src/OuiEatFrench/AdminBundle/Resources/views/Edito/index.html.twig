Index des Edito
<a href={{ path('oui_eat_french_admin_edito_create') }}>Ajouter</a>
<table class="table table-hover">
    <thead>
    <tr>
        <td>Image</td>
        <td>Titre</td>
        <td>text</td>
        <td>Actions</td>
    </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td style="width: 15%; height: 10%;"><img style="width: 50%; height: 50%;" src="{{ asset("OEF/img_admin_edito/" ~ entity.imageName) }}" alt="{{ entity.imageName }}"></td>
            <td>{{ entity.title }}</td>
            <td>{{ entity.text }}</td>
            <td>
                <ul>
                    <li><a href={{ path('oui_eat_french_admin_edito_edit', {'id' : entity.id}) }}>Edition</a></li>
                    <li><a class='delete' href={{ path('oui_eat_french_admin_edito_delete', {'id' : entity.id}) }}>Supprimer</a></li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
