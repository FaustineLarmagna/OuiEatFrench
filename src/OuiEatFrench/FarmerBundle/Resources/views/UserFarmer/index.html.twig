Index producteurs 
<br><br>
<a href={{ path('oui_eat_french_user_farmer_create') }}>Ajouter</a>
<table class="table table-hover">
    <thead>
    <tr>
        <td>Nom</td>
        <td>Actions</td>
    </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td>{{ entity.firstname }} {{ entity.lastname }}</td>
            <td>
                <ul>
                    <li><a href={{ path('oui_eat_french_user_farmer_show', {'id' : entity.id}) }}>Voir</a></li>
                    <li><a href={{ path('oui_eat_french_user_farmer_edit', {'id' : entity.id}) }}>Edition</a></li>
                    <li><a class='delete' href={{ path('oui_eat_french_user_farmer_delete', {'id' : entity.id}) }}>Supprimer</a></li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
